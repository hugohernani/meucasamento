$("#gallery-images-container").html("<%= j render(partial: 'wedding/events/list_gallery_images', locals: {gallery_images: @gallery_images}) %>");

$(".flickr_pagination a").each(function(e){
  $(this).attr('data-remote', true);
  $(this).ajaxSend(function(){
    Pace.options.ajax = false;
  }).ajaxComplete(function(){
    Pace.options.ajax = true;
  });
})

$("#gallery-nav-link").click();
